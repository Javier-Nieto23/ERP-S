# Copy this file to .env and fill values before running init_db.js
PGUSER=postgres
PGPASSWORD=your_db_password_here
PGHOST=localhost
PGPORT=5432
PGDATABASE=portal_db
JWT_SECRET=change_this_secret
# Optional: set PORT for the backend server
PORT=3000
